<UserControl
    x:Class="Sudoku.Cell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudoku"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <local:SelectedCellConverter x:Key="SelectedCellConverter"/>
    </UserControl.Resources>

    <Grid Width="50" Height="50" >
        <Button x:Name="btn" BorderBrush="Black" Width="50" Height="50" Background="{x:Bind isSelected, Converter={StaticResource SelectedCellConverter},Mode=OneWay}" BorderThickness="1" Click="Button_Click">
            <TextBlock x:Name="tb" TextAlignment="Center" FontSize="25" FontFamily="Gill Sans MT" VerticalAlignment="Center" Foreground="Black"/>
        </Button>
    </Grid>
</UserControl>